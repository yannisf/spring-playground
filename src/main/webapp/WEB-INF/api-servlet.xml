<?xml version="1.0" encoding="UTF-8" ?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:amq="http://activemq.apache.org/schema/core"
       xmlns:jms="http://www.springframework.org/schema/jms"
       xmlns="http://www.springframework.org/schema/beans"
       xmlns:camel="http://camel.apache.org/schema/spring"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
    http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
    http://activemq.apache.org/schema/core http://activemq.apache.org/schema/core/activemq-core.xsd
    http://www.springframework.org/schema/jms http://www.springframework.org/schema/jms/spring-jms.xsd
    http://camel.apache.org/schema/spring http://camel.apache.org/schema/spring/camel-spring.xsd
    http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd">


    <context:component-scan base-package="fraglab"/>
    <mvc:annotation-driven/>

    <!-- Enable JMS annotations -->
    <jms:annotation-driven/>

    <!-- Setup an embedded ActiveMQ instance -->
    <amq:broker id="broker" useJmx="false" persistent="false">
        <amq:transportConnectors>
            <amq:transportConnector uri="tcp://localhost:0"/>
        </amq:transportConnectors>
    </amq:broker>

    <!-- Define the destination -->
    <amq:queue id="destination" physicalName="fraglab.jms"/>

    <!-- Define the ActiveMQ connection factory -->
    <amq:connectionFactory id="amqConnectionFactory" brokerURL="vm://localhost" trustAllPackages="true"/>

    <!-- Wrap connection factory with a Spring JMS connect connection factory -->
    <bean id="jmsConnectionFactory" class="org.springframework.jms.connection.SingleConnectionFactory">
        <property name="targetConnectionFactory" ref="amqConnectionFactory"/>
        <property name="reconnectOnException" value="true"/>
    </bean>

    <!-- Create a JMS listener container factory -->
    <bean id="jmsListenerContainerFactory" class="org.springframework.jms.config.SimpleJmsListenerContainerFactory">
        <property name="connectionFactory" ref="jmsConnectionFactory"/>
    </bean>

    <!-- Define Spring the JMS template -->
    <bean id="jmsTemplate" class="org.springframework.jms.core.JmsTemplate">
        <property name="connectionFactory" ref="jmsConnectionFactory"/>
        <property name="defaultDestination" ref="destination"/>
    </bean>

    <camel:camelContext id="camel">
        <camel:package>fraglab.application.camel</camel:package>
        <camel:jmxAgent id="agent" createConnector="true"/>
    </camel:camelContext>

</beans>
